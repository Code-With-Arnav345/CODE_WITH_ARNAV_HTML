** Practice Set **

**1.**
Create a basic HTML page with a title `"Class Notes Practice"` and display the text `"Hello, Arnav!"` inside a `<p>` tag.

---

**2.**
Add a heading structure from `<h1>` to `<h6>` showing the hierarchy of headings.

---

**3.**
Create a paragraph using the `<p>` tag. Inside the paragraph:

* Make some words **bold**, *italic*, and <u>underlined</u>.

---

**4.**
Insert an image using the `<img>` tag with:

* A placeholder URL (`https://via.placeholder.com/150`)
* An `alt` attribute
* Width = 150px, Height = 150px

---

**5.**
Create a link (`<a>`) to `https://openai.com` that:

* Opens in a **new tab**
* Displays the text “Visit OpenAI”

---

**6.**
Make a form with:

* A text input for name
* A radio input for gender (Male / Female)
* A checkbox for “Subscribe to newsletter”
* A dropdown `<select>` for city: Delhi, Mumbai, Bangalore
* A textarea for feedback
* A submit button

---

**7.**
Create a table with:

* A caption: `"Student Info"`
* 2 rows, 3 columns (`Name`, `Roll No`, `City`)
* Use `<thead>` and `<tbody>` properly

---

**8.**
Create a layout using semantic tags:

* `<header>` with site title
* `<nav>` with 3 links
* `<main>` with one `<section>` and one `<article>`
* `<footer>` with copyright

---

**9.**
Create a list:

* Unordered list of 3 fruits
* Ordered list of 3 coding languages

---

**10.**
Embed a YouTube video using an `<iframe>` and set:

* Width = 560
* Height = 315
* Allow fullscreen